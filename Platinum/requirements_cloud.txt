# Platinum Cloud Requirements (Oracle VM)
# Install: pip install -r requirements_cloud.txt

# Gmail API
google-auth>=2.22.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1
google-api-python-client>=2.100.0

# HTTP requests (health monitor WA alerts, future integrations)
requests>=2.28.0

# Standard lib only — no extra deps needed for:
# - base_watcher, retry_handler, audit_logger
# - file_watcher, email_drafter, social_drafter
# - health_monitor (uses subprocess + shutil)
# - sync_agent (uses subprocess for git)
# - orchestrator (uses subprocess + signal)
