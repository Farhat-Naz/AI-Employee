# Platinum Local Requirements (Windows PC)
# Install: pip install -r requirements_local.txt

# WhatsApp Green API
requests>=2.28.0

# Standard lib only — no extra deps for:
# - base_watcher, retry_handler, audit_logger
# - whatsapp_watcher (uses requests)
# - filesystem_watcher (uses shutil, mimetypes)
# - approval_agent (uses shutil)
# - watchdog (uses subprocess)

# Phase 2 — uncomment when ready:
# tweepy>=4.14.0          # Twitter/X
# facebook-sdk>=3.1.0    # Facebook
# google-api-python-client>=2.100.0  # Gmail send via MCP
